spring.application.name=pharmacy
# Standardize on port 8080 to match Dockerfile and best practices
server.port=8080
spring.main.allow-bean-definition-overriding=true
spring.jpa.hibernate.ddl-auto=update

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:oracle:thin:@localhost:1523/XEPDB1}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:PHARMACY_LOCAL}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:devpass}
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# ===================================================================
# EXTERNALIZED SECURITY & JWT CONFIGURATION
# ===================================================================
jwt.secret=${JWT_SECRET:SigoDV8lfIUvOF0tPRfOGGpxj94jPRz2d7VHzNzSr/8=}
jwt.expirationMs=${JWT_EXPIRATION_MS:86400000}

# ===================================================================
# EXTERNALIZED EMAIL CONFIGURATION
# ===================================================================
spring.mail.host=${MAIL_HOST:smtp.mail.yahoo.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:nightowlgt@yahoo.com}
spring.mail.password=${MAIL_PASSWORD:xofqfxrqnuphfjzx}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
admin.notification.email=${ADMIN_NOTIFICATION_EMAIL:nightowlgt@yahoo.com}

# ===================================================================
# EXTERNALIZED ENVIRONMENT URLS
# ===================================================================
app.base-url=${APP_BASE_URL:http://localhost:8080}
app.frontend-url=${APP_FRONTEND_URL:http://localhost:4300}

# ===================================================================
# LOGGING & MANAGEMENT 
# ===================================================================
management.endpoints.web.exposure.include=*
logging.level.org.springframework.web=INFO
logging.level.com.example.pharmacy=INFO
